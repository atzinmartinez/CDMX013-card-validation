import validator from './validator.js';


const numberCard = document.getElementById ("numberCard");
const enviar=document.getElementById ("enviar");

console.log ("Hola");

/*enviar.addEventListener("click",function(event){
    event.preventDefault ();
    const tarjeta = numberCard.value
    const revertido = tarjeta.split("").reverse()
    console.log (revertido);
    for (let contador = 0; contador<revertido.length);
   
    
})
 */
    
//validator.isValid(numberCard.value)




/* hacer un arreglo con el numero y despues hacer el buble
como sumar numeros en un arreglo


caputar el input separarlo
sumarlo

/* 









/*
// para llamar a un elemento de html se debe poner (" ")
const botonValidar = document.getElementById("boton");

//al poner .value toma el solo el valor no todo el nodo completo

botonValidar.addEventListener("click", function (event){
    //se agrega event cuando se añade un addEventlistener para darle una instruccion exacta
    event.preventDefault ();
    const ingresarTarjeta = document.getElementById("numberCard").value;
    console.log (ingresarTarjeta);


    //se crea objeto y se le da función isValid
    validator.isValid (ingresarTarjeta);
});




//function mostrarNumero (){


*/














//funcion que traiga el valor de input
//como leer el valor de un input
//como modificar el valor de un input o etiqueta con JS

//darle funcionalidad a un boton asignarle una funcioin a un boton

//solo inves

//como juntar index y validator

//usar una condicional para que no acepte meno de 16 digitos
